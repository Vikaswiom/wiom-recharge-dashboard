services:
  - type: web
    name: wiom-recharge-dashboard
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 600
    envVars:
      - key: METABASE_API_KEY
        sync: false
